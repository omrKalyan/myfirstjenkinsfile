pipeline{
    agent any
    stages {

    stage('build'){
    steps{
    sh 'ant -f build.xml -v'
    }
    }
    stage ('test') {
    steps{
    echo "This is blueocean"
    }
    }
    post {
   	 always{
   		 archiveArtifacts artifacts: 'dist/*.jar', fingerprint: true
   	 }
    }
}

